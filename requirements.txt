# ---- Phase 1: beat-sync auto-cut engine ----
numpy
librosa>=0.10
soundfile
opencv-python-headless>=4.8
PyYAML

# ---- Phase 2: captions ----
faster-whisper>=1.0          # CUDA build of ctranslate2 is pulled in automatically;
                             # needs NVIDIA driver + cuBLAS/cuDNN for GPU mode.

# ---- Phase 4: AI content generator ----
# (no extra deps — talks to local Ollama via stdlib urllib)

# ---- Phase 5: web UI ----
fastapi
uvicorn[standard]
python-multipart

# ---- Phase 7: AI image generation for "no raw footage" reels (optional) ----
# CPU-only fallback (slow, works everywhere):
#   pip install diffusers accelerate torch --index-url https://download.pytorch.org/whl/cpu
# CUDA (RTX 4070 Laptop — fast, recommended):
#   pip install diffusers accelerate
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
# diffusers
# accelerate
